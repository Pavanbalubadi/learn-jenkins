## Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { node { label 'workstation' } }
    stages {
        stage('pavan') {
          echo 'Hello Pavan'
            steps {
                sh 'env'
            }
        }
    }
}