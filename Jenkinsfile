// Jenkinsfile (Declarative Pipeline)

pipeline {
    agent node { lable 'workstation'}

    stages {
        stage('Hello') {
            steps {
                echo 'Hello Pavan'
            }
        }
    }
}
//
